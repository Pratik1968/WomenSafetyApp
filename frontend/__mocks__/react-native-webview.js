// Auto-applied manual mock — the real package isn't transformed in jest.
const React = require("react");
const WebView = () => React.createElement("WebView", null);
module.exports = { WebView, default: WebView };
